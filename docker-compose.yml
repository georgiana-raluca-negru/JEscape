services:
  db:
    image: mysql:8
    environment:
      MYSQL_DATABASE: jescape
      MYSQL_USER: jescape_user
      MYSQL_PASSWORD: jescape_password
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
